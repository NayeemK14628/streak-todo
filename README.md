# Streak Todo

A responsive React + Firebase to-do app with recurring weekly tasks, daily completion, streak tracking, Google login, cloud sync, and PWA installation.

## Run locally
1. Install Node.js.
2. Copy `.env.example` to `.env` and add Firebase values.
3. Run `npm install`.
4. Run `npm run dev`.

## Build
Run `npm run build`.
